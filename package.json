{
  "name": "tradecycle-arc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "verify:contracts": "npm run compile",
    "verify:ui:lint": "cd rwa-ui && npm run lint",
    "verify:ui:build": "cd rwa-ui && npm run build"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.3.1",
    "fs-extra": "^11.3.4"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.5.0",
    "hardhat": "^2.22.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
